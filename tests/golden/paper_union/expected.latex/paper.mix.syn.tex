\begin{codeblock}
template<class T>
class @\libglobal{mixer}@ {
public:
  // \ref{paper.mix}, mixing
  int mix(const T& part) const
    requires(alpha_policy<T> == 0) &&
            requires(const blend_policy& policy) { policy.strength(); };
};
\end{codeblock}
