specgen: mixer/synopsis: error: `blend_policy` appears in rendered output but is not a documented entity (declared in `spec_paper_sibling.hpp`, which this run does not document): mark it `\expos` where it is declared, document it in this run, render the paper's documents together (`render` takes `--from-ir` once per header, and validates across their union), or rewrite the wording in documented terms
specgen: paper.mix/constraints: error: `blend_policy` appears in rendered output but is not a documented entity (declared in `spec_paper_sibling.hpp`, which this run does not document): mark it `\expos` where it is declared, document it in this run, render the paper's documents together (`render` takes `--from-ir` once per header, and validates across their union), or rewrite the wording in documented terms
